# .env.example
# This file is intentionally left empty as the application uses client-side localStorage for data persistence and does not require server-side environment variables for database connections or API keys.
